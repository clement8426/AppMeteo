<li class="m-1 list-item" style="none">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <img src="https://flagcdn.com/w20/<%= visitor.countryCode.downcase %>.png" srcset="https://flagcdn.com/w40/<%= visitor.countryCode.downcase %>.png 2x" width="20" alt="<%= visitor.country %>">
            <%= visitor.city %>
        </div>
        <div><strong class="small"><%= visitor.temperature %>°</strong></div>
        <div class="small"><%= visitor.weatherCondition %></div>
        <div class="small"><%= visitor.dayOrNight %></div>
        <div class="small" id="timer_<%= visitor.id %>" data-created-at="<%= visitor.created_at %>"></div>
    </div>
    <div class="d-none"><%= visitor.created_at %></div>
</li>
